<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.LinearLayoutCompat xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center">

    <!-- TextView لعرض المعادلة -->
    <TextView
        android:id="@+id/textEquation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="24sp"
        android:textColor="#000000"
        android:text="المعادلة هنا"
        android:layout_marginBottom="30dp" />

    <!-- TextView لعرض الإجابة -->
    <TextView
        android:id="@+id/textAnswer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="24sp"
        android:textColor="#000000"
        android:text=""
        android:layout_marginBottom="30dp" />

    <!-- Button لتوليد معادلة جديدة -->
    <Button
        android:id="@+id/buttonGenerate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="New equation"
        android:onClick="generateNewEquation"
        android:layout_marginTop="10dp" />

    <!-- Button لعرض الإجابة -->
    <Button
        android:id="@+id/buttonShowAnswer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Show answer"
        android:onClick="showAnswer"
        android:layout_marginTop="10dp" />

</androidx.appcompat.widget.LinearLayoutCompat>
